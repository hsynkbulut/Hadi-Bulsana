// List of Enums
// They cannot be created inside a class.

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, ldelivered }

enum PaymentMethods {
  paypal,
  googlePlay,
  applePlay,
  visa,
  masterCard,
  creditCard,
  paystack,
  razorPay,
  paytm
}
